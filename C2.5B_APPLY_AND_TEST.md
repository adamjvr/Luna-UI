# Apply and validate Luna C2.5B

```bash
cd ~/GitHub/Luna-UI
unzip -o ~/Downloads/Luna-UI-C2.5B-Repository-Overlay.zip -d .
git diff --check
swift test --filter LunaC25BSingleShapeWrapIndexTests
swift build
swift test
./scripts/validate-iteration.sh
```

Commit only after graphical validation:

```bash
git add Sources/LunaUI/LunaStaticTextWrapIndex.swift \
        Tests/LunaUIPhase3CTests/LunaC25BSingleShapeWrapIndexTests.swift \
        docs/C2.5B_SINGLE_SHAPE_WRAP_INDEX.md \
        C2.5B_APPLY_AND_TEST.md
git diff --cached --check
git commit -m "feat(text): add C2.5B single-shape wrap index"
git push origin a1.1-measured-audit
```

# Apply Luna C2.5C

```bash
cd ~/GitHub/Luna-UI
unzip -o ~/Downloads/Luna-UI-C2.5C-Repository-Overlay.zip -d .
git diff --check
swift test --filter LunaC25CVisualRowIndexTests
swift build
swift test
./scripts/validate-iteration.sh
```

Commit message:

```bash
git commit -m "feat(text): add C2.5C indexed visual-row planning"
```
